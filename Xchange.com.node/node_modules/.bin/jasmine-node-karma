#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jasmine-node-karma/bin/jasmine-node-karma" "$@"
  ret=$?
else 
  node  "$basedir/../jasmine-node-karma/bin/jasmine-node-karma" "$@"
  ret=$?
fi
exit $ret
